tests require the following to be installed:

firefox
python
sudo apt-get install python-pip
sudo pip install selenium
sudo pip install pytest
sudo pip install pytest-xdist
sudo pip install pytest-html

Then run as:
py.test -n 2 --html=report.html

Can be run remotely or locally since it just launches firefox and controls it
